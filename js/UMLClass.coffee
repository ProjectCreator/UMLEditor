class App.UMLClass

    # CONSTRUCTOR
    constructor: (name, attributes, methods, model) ->
        @name       = name
        @attributes = attributes
        @methods    = methods
        @model      = model

    
