class App.View

    # CONSTRUCTOR
    constructor: (umlClass, container) ->
        @umlClass = umlClass
        @container = container

    draw: () ->
        return @

    redraw: (properties) ->
        return @
