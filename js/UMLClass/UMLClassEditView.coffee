class App.UMLClassEditView extends App.AbstractView

    # CONSTRUCTOR
    constructor: (model, container) ->
        super(model, container)

    draw: () ->
        return @

    redraw: (properties) ->
        return @
